const Explosion = {
	schema: {
		range: { default: 1 },
		color: { default: "#FF0000" }
	},

	// LIFECYCLE JAZZ
	// --------------------------------
	init(){

	}, // init
};

export default Explosion;